<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <link rel="Stylesheet" href="css/styles.css">
    <style>
        body{
            background-image:url("images/editback.jpg");
            background-repeat: no-repeat;
            background-size: cover;
        }
        #image{
            object-fit: cover;
        }
        #inside{
            margin:50px;
            size: 30%;
        }
        #big{
            background-color:rgb(207, 229, 237);
            border-radius:5px;
            margin-left:220px;
            margin-top:100px;
            border:2px solid black;
            padding:30px;
            height:90%;
            width:700px;
            display:flex;
        }
        label{
            margin:5px;
            padding:5px;
            display: inline-flex;
            cursor:text;
        }
        input[type="button"]{
            margin:15px;
            text-align:center;
            width:50%;
            height:40px;
            cursor:pointer;
            border-radius: 3px;
        }
        #profile{
            border-radius:5px;
            width:200px;
            height:200px;
        }
        a{
            color: black;
        }

        #inside {
    margin: 50px;
    size: 30%;
    display: flex;
    flex-direction: column;
    text-align:left;
}

label {
    margin: 5px;
    padding: 5px;
    display: inline-flex;
    cursor: text;
    color:brown;
    font-weight:bold;
}

span {
    margin-left: 10px; /* Adjust as needed */
}

    </style>
</head>
<body>
    <div>
        <a href="/home">Home</a>
        <!-- <a href="/">About</a> -->
        <a href="/contact">Contact Us</a>
        <a class = "nav" href="/change-password"> Change Password </a>
        <a href="/profile">Profile </a>
        <a href="/logout">LogOut</a>
    </div>
    
    <div id="big">
        <div id="image">
            <img src="<%= user.profilePicUrl %>" id="profile" alt="Profile"/><br>
            <input type="button" onclick='window.location.href="/edit"' value="Edit details">
            <input type="button" onclick="window.location.href='/changeprofile'" value="Change Profile Pic">
        </div>
        <div id="inside">
            <label>Email:</label><span><%= user.email %></span><br>
            <label>Username:</label><span><%= user.username %></span><br>
            <label>Gender:</label>
                <span>
                    <% if (user.gender === 'M') { %>
                        Male
                    <% } else if (user.gender === 'F') { %>
                        Female
                    <% } else { %>
                        Other
                    <% } %>
                </span><br>
            <label>Diet:</label><span><%= user.dietChoice %></span><br>
        </div>
        
    </div>
</body>
</html>